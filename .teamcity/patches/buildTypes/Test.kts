package patches.buildTypes

import jetbrains.buildServer.configs.kotlin.*
import jetbrains.buildServer.configs.kotlin.ui.*

/*
This patch script was generated by TeamCity on settings change in UI.
To apply the patch, change the buildType with id = 'Test'
accordingly, and delete the patch script.
*/
changeBuildType(RelativeId("Test")) {
    dependencies {
        remove(AbsoluteId("MavenSample_SheepProject_FailingCountingTests")) {
            snapshot {
            }
        }

        add(RelativeId("Up")) {
            snapshot {
            }
        }

        add(AbsoluteId("Aqa1296723421_Build")) {
            snapshot {
            }
        }

        add(AbsoluteId("Aqa1296723421_Db480261621")) {
            snapshot {
            }
        }

    }
}
